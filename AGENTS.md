Always compile after changes.

## FIRST ACTION — MANDATORY
At the very start of EVERY conversation — before answering, before using any tool, even if the answer seems obvious from context — call tool_search_tool_regex with pattern `^mcp_context-mode_` to load ctx tools. This is unconditional. Do NOT skip it even for simple questions.